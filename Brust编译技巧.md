来源于川哥讲的豆知识，具体的可能还需要去查资料



# loop（循环）优化

在函数前面加上`[BrustCompile]`

函数内不能有分支语句

会将循环进行向量化展开，机器码一次操作多个位实现加速，有点像GPU编程

可以通过引入类似断言的机制来检查是否可以向量化展开

好处是减少`Jump`，加快运行速度



# NoAlias

在变量声明前面加上`[NoAlias]`告诉编译器这个变量没有别名，这样编译器会检查能否直接传值，而不是先存到内存里，下次用的时候再加载到寄存器。